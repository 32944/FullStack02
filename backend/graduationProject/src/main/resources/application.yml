server:
  port: 8080

spring:
  application:
    name: graduationProject
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/job_wise_guide?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: ywy2003nljy
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000
  data:
    redis:
      host: localhost
      port: 6379
      password: ywy2003nljy
  cache:
    type: redis
    redis:
      time-to-live: 600000

mybatis:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.itheima.graduation.entity

jwt:
  secret: job_wise_guide_secret_key_2024_must_be_at_least_32_chars
  expiration: 86400000

# 微信小程序配置
wx:
  appid: wx801ad03a3ed04f7e
  secret: 26e92181b49cffe203130f3eff21fee8
